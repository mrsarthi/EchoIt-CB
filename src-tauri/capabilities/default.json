{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. core:window:allow-close is needed so the exit prompt can actually close the app when the user confirms leaving from the back button.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:allow-close"
  ]
}
